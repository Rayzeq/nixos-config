diff --git a/Cargo.lock b/Cargo.lock
index 8859b55c..8f692fbf 100644
--- a/Cargo.lock
+++ b/Cargo.lock
@@ -8964,6 +8964,7 @@ dependencies = [
  "base64 0.22.1",
  "byteorder",
  "bytes 1.7.2",
+ "cfg-if 1.0.0",
  "chrono",
  "daedalus",
  "dashmap 6.1.0",
@@ -8986,6 +8987,7 @@ dependencies = [
  "serde_json",
  "sha1_smol",
  "sha2 0.10.8",
+ "shlex",
  "sqlx",
  "sys-info",
  "sysinfo",
diff --git a/packages/app-lib/Cargo.toml b/packages/app-lib/Cargo.toml
index 4681899e..0affea1b 100644
--- a/packages/app-lib/Cargo.toml
+++ b/packages/app-lib/Cargo.toml
@@ -19,6 +19,8 @@ async_zip = { version = "0.0.17", features = ["chrono", "tokio-fs", "deflate", "
 flate2 = "1.0.28"
 tempfile = "3.5.0"
 urlencoding = "2.1.3"
+cfg-if = "1.0.0"
+shlex = "1.3.0"
 dashmap = { version = "6.0.1", features = ["serde"] }
 
 chrono = { version = "0.4.19", features = ["serde"] }
diff --git a/packages/app-lib/src/launcher/mod.rs b/packages/app-lib/src/launcher/mod.rs
index 1ddb9414..106eaabc 100644
--- a/packages/app-lib/src/launcher/mod.rs
+++ b/packages/app-lib/src/launcher/mod.rs
@@ -7,11 +7,8 @@ use crate::state::{
     Credentials, JavaVersion, ProcessMetadata, ProfileInstallStage,
 };
 use crate::util::io;
-use crate::{
-    process,
-    state::{self as st},
-    State,
-};
+use crate::{process, state as st, State};
+use cfg_if::cfg_if;
 use chrono::Utc;
 use daedalus as d;
 use daedalus::minecraft::{RuleAction, VersionInfo};
@@ -521,7 +518,17 @@ pub async fn launch_minecraft(
     let args = version_info.arguments.clone().unwrap_or_default();
     let mut command = match wrapper {
         Some(hook) => {
-            wrap_ref_builder!(it = Command::new(hook) => {it.arg(&java_version.path)})
+            cfg_if! {
+                if #[cfg(unix)] {
+                    let cmd = shlex::split(hook).expect("invalid input command");
+                    wrap_ref_builder!(it = Command::new(cmd[0].clone()) => {
+                        it.args(&cmd[1..]);
+                        it.arg(&java_version.path);
+                    })
+                } else {
+                    wrap_ref_builder!(it = Command::new(hook) => {it.arg(&java_version.path)})
+                }
+            }
         }
         None => Command::new(&java_version.path),
     };

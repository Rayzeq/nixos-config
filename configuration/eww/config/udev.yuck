(deflisten udev
  :initial 0
  "python ./scripts/udev.py"
)

(defwidget brightness []
  (eventbox
    :class "brightness"
    :onscroll 'if [ "{}" = "up" ]; then brightnessctl set +5%; else brightnessctl set 5%-; fi'
    (label :text "󰖙  ${udev}%")
  )
)